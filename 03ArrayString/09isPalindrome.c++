#include<iostream>
#include<cstring>
using namespace std;
int main()
{
    char sentence[100];
    cin.getline(sentence,100);

    int str_len = strlen(sentence);
    char temp[100];
    strcpy(temp,sentence);
    while(str_len--)
    { 
        
    }

    return 0;
}